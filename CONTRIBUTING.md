# Contributing

1. Install .NET SDK from `global.json`
2. `./scripts/build.ps1` and `./scripts/test.ps1` must pass with zero warnings
3. Keep Core free of WPF/Win32 references
4. Do not add drawing persistence or network dependencies without explicit approval
