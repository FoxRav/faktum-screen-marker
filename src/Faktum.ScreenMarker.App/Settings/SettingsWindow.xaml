<Window x:Class="Faktum.ScreenMarker.App.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource SettingsTitle}"
        Height="320"
        Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
      <TextBlock VerticalAlignment="Center" Text="{DynamicResource SettingsPrimaryHotkey}" Margin="0,0,8,0"/>
      <TextBlock VerticalAlignment="Center" Text="Ctrl+§" FontWeight="SemiBold"/>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
      <TextBlock VerticalAlignment="Center" Text="{DynamicResource SettingsFallbackHotkey}" Margin="0,0,8,0"/>
      <TextBlock VerticalAlignment="Center" Text="Ctrl+Shift+F12"/>
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
      <TextBlock VerticalAlignment="Center" Text="{DynamicResource SettingsLanguage}" Margin="0,0,8,0"/>
      <ComboBox x:Name="LanguageCombo" Width="160">
        <ComboBoxItem Content="English (en-US)" Tag="en-US"/>
        <ComboBoxItem Content="Suomi (fi-FI)" Tag="fi-FI"/>
      </ComboBox>
    </StackPanel>

    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="{DynamicResource SettingsSave}" Width="100" Margin="0,0,8,0" IsDefault="True" Click="SaveButton_Click"/>
      <Button Content="{DynamicResource SettingsCancel}" Width="100" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
